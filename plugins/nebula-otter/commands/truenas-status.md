# /truenas-status

Show a compact dashboard: system info + all apps grouped by state.

## Usage
/truenas-status

## What this does
Calls `get_system_info` then `list_apps` (no filter), groups results by state.
